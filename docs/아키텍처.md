# 아키텍처 정의서

> **작성·동기화 메타정보**
> 
> 
> Notion 원본 URL: https://app.notion.com/p/3cd73873401a80d9be4ee24c173805a4
> 
> Snapshot 기준 시점: `Sprint 2 Review 종료 시점 (2026-09-11)`
> 
> 동기화 시각: `2026-09-14 10:00 KST`
> 
> 직접 편집 금지: Git Snapshot은 직접 편집하지 않고 Notion 원본을 수정한 뒤 다시 동기화합니다.
> 

> 관련 사용자 시나리오: [요구사항 링크]
관련 경계: [서비스경계 절 링크]
> 

## 현재 실행 구조

```mermaid
flowchart LR
    U[방문자·참가자·주최자·전체관리자] --> GW[Gateway]
    GW --> MS[Member-Service]
    GW --> CS[Conference-Service]
    GW --> RS[Reservation-Service]
    RS -->|세션 정원 조회 동기 계약| CS
    CS -->|참석자 체크인 집계 조회(Sprint 3, getAttendeeCheckinStats)| RS
    CS -->|참석자 통계 AI 요약 생성(Sprint 3)| AI[Claude API 외부]
    MS --> MDB[(Member DB)]
    CS --> CDB[(Conference DB)]
    RS --> RDB[(Reservation DB)]
```

| Component | 책임 | 소유 데이터 | 관련 Story·계약 | 상태 | 추가·변경 Sprint |
| --- | --- | --- | --- | --- | --- |
| Gateway | 외부 요청 라우팅, 1차 인증 검증, CORS, Trace ID 생성·전파, `/internal/v1/**` 비공개 | 없음 | Story 1 Task 1-1(`#2`) — Gateway 인증·조회 Route 구성 | PLANNED | Sprint 1 |
| Member-Service | 회원가입/로그인, 계정·Role(참가자/주최자/전체관리자) 관리, 주최자 자체가입 시 사업자등록번호 형식 검증 | member(계정, Role, scope, organizationName, businessNo) | Story 2 Task 2-1(`#9`) `POST /api/members/signup`, `POST /api/auth/login` · Story 8 Task 8-1(`#30`) 로그인 흐름 · Story 3(구 Story 13, `#26`) `signupOrganizer — POST /api/members/organizers/signup`(舊 관리자가 발급하던 `POST /api/members/organizers` 계약은 폐기) | PLANNED | Sprint 1 (Sprint 2에 Story 3(13) 주최자 자체 회원가입으로 확장) |
| Conference-Service | 컨퍼런스·세션 CRUD, 컨퍼런스·세션 승인/반려 처리(권한검증 포함), 세션 정원·일정 관리, 컨퍼런스 참석자 통계(AI 요약) 생성 | conference, session(정원·일정·상태), conference_attendee_summary | Story 1 Task 1-2(`#4`) `GET /api/conferences`, `GET /api/conferences/{id}` · Story 9 Task 9-1(`#36`) 컨퍼런스 등록 신청 API · Story 6(구 Story 14, `#27`) `listPendingConferences`/`approveConference`/`rejectConference` · Story 8(`#68`) `listPendingSessions`/`approveSession`/`rejectSession` · Story 7(구 Story 10) 세션 정원·신청기간 등록·수정(주최자가 승인된 컨퍼런스에 세션을 만들고 정원·기간을 설정, API 계약은 후속 설계) · Story 15 getAttendeeSummary/getAttendeeCheckinStats | PLANNED | Sprint 1 (Sprint 2에 Story 6·7·8 컨퍼런스·세션 승인 및 세션 정원·일정 설정으로 확장), Sprint 3에 Story 15 참석자 통계로 확장 예정 |
| Reservation-Service | 세션 신청 접수, 정원 검증·홀드, 대기열 순번, 결제 연동, QR 발급, 취소·환불 | reservation, waiting_queue, qr_ticket, booth_visit, pg_credential | Story 3 Task 3-1(`#21`)·3-2(`#23`) 세션 정원 조회 동기 계약 · Story 4 Task 4-1(`#7`) 대기열 등록/순번 조회 | PLANNED | Sprint 1 (Sprint 2에 Story 11(구 Story 5) 결제·QR, Sprint 3에 Story 12·13(구 Story 6·7) 취소환불·QR입장으로 확장 예정) |

## 결정 기록

| 결정 | 근거가 된 사용자 시나리오·위험 | 선택 | 검토 대안 | 결정 Sprint |
| --- | --- | --- | --- | --- |
| 모든 외부 요청은 Gateway를 단일 진입점으로 통과한다 | 전체 시나리오 공통 — 인증·CORS·Trace ID를 서비스마다 중복 구현하면 정책이 서비스별로 어긋날 위험 | Gateway 단일 진입점 + `/internal/**` 비공개 | 서비스별 직접 외부 노출 — 인증 로직 중복, CORS 관리 분산으로 기각 | Sprint 1 |
| 서비스별 DB를 완전히 분리하고 직접 DB 접근·JOIN을 금지한다 | [소유자원-접근제한] 규칙, 서비스 간 강결합 방지 | 서비스당 전용 DB, 논리적 ID(`memberId`·`conferenceId`·`reservationId`) 참조만 허용 | 공유 DB·직접 JOIN — 배포 독립성 상실, 한 서비스 스키마 변경이 전체에 영향을 주는 위험으로 기각 | Sprint 1 |
| Admin 전용 기능은 별도 오케스트레이션 서비스 없이 각 도메인 서비스가 직접 구현한다 | 강사님 피드백(“Admin이 필요하면 해당 서비스에서 엔드포인트 직접 제작”) — 별도 Admin-Service는 불필요한 홉과 배포 단위만 늘림 | Member-Service(계정 생성)·Conference-Service(승인/반려)가 각자 권한검증까지 직접 구현 | Admin-Service 오케스트레이션(Sprint 1 초기 결정) — 권한검증 중복·감사 어려움 우려로 도입했으나 실제로는 불필요한 서비스 간 홉만 추가한다고 판단해 철회 | Sprint 1 (재검토) |
| 서비스 간 동기 호출 실패 시 Fail-closed로 처리한다 | [정원-초과-금지], [승인-전-비공개] — 실패 시 임의로 허용 처리하면 정원 초과·미승인 컨퍼런스 노출 위험 | Timeout(3s/5s) 초과·오류 시 503 반환, 상태 변경·좌석 확정 안 함 | Fail-open(임의 성공 처리) — 데이터 정합성이 깨질 위험으로 기각 | Sprint 1 |
| 세션 정원 검증은 신청 시점에 동기 API로 실시간 확인한다 | 시나리오 3·4, [정원-초과-금지] — 동시 신청 상황에서 최종 일관성만으로는 초과 판매 위험 | Reservation-Service → Conference-Service 동기 호출(세션 정원 조회 계약, Task 3-2 `#23`) | 이벤트 기반 비동기 동기화(최종 일관성) — 동시성 상황에서 정원 초과 방지를 보장하기 어려워 기각 | Sprint 1 |
| 주최자 계정은 전체관리자가 발급하지 않고, 사업자등록번호 형식 검증을 통과하면 즉시 자체 가입으로 생성한다 | Story 3(13), [사업자번호-인증필수]·[주최자가입-중복금지] 규칙 — 관리자 승인 대기 병목 없이 온보딩해야 함 | 자체 가입 엔드포인트에서 형식 검증(정규식)만 수행, 통과 시 즉시 Role=ORGANIZER 부여 | 전체관리자가 계정을 생성해 발급하는 방식(Sprint 1 초기 설계) — 관리자 병목·온보딩 지연 우려로 기각, Sprint 2에서 폐기 | Sprint 2 |
| 사업자등록번호 진위 확인 방식은 아직 정하지 않았다 | Story 3(13), [사업자번호-인증필수] — 형식 검증만으로는 실제 사업자 여부를 보증하지 못해 주최자 가입 어뷰징 가능(Sprint 1 리뷰 피드백) | 미정 — Sprint 2 내 확정 예정 | 국세청 사업자등록정보 진위확인 API 연동 / 형식 검증만 하는 Mock 유지, 두 안을 검토 중 | Sprint 2 |
| 결제 대기 중인 홀드(HOLD)에 유효시간(TTL)을 도입해 자동 만료시킨다 | Sprint 1 리뷰 피드백 — 홀드가 무기한 유지되면 대기열 인원에게 좌석이 넘어가지 않아 매진 세션에서 실사용 시 심각한 문제 발생 | 미정 — 구체적 만료 시간·만료 트리거(스케줄러 vs 조회 시점 lazy 만료)는 Sprint 2 내 확정 예정 | 무기한 홀드 유지(Sprint 1 현행) — 좌석 회전이 막혀 대기열이 진행되지 않는 문제로 Sprint 2에서 폐기 예정 | Sprint 2 |
| 컨퍼런스·세션 승인 기준은 아직 명문화되지 않았다 | Story 6(14)·Story 8, Sprint 1 리뷰 피드백 — 현재는 승인·반려 판단 기준 없이 SQL로 수동 승인 중이라 재현성·감사 가능성이 없음 | 미정 — 승인 기준 문서화 후 Admin 승인 API(`listPendingConferences`/`approveConference`/`rejectConference`, `listPendingSessions`/`approveSession`/`rejectSession`)에 반영, Sprint 2 내 확정 예정 | SQL 수동 승인 유지(Sprint 1 현행) — 판단 기준·이력이 남지 않아 감사 불가로 Sprint 2에서 폐기 예정 | Sprint 2 |
| 세션 정원·신청기간 등록 값은 별도 게이트 없이 주최자가 승인된 컨퍼런스 범위 안에서 직접 설정한다 | Story 7(10), [세션정원-유효성] 규칙 — 세션은 컨퍼런스 하위 자원이므로 컨퍼런스 승인과 별개의 추가 승인 절차를 두면 온보딩이 지연됨 | Conference-Service가 소유자원 검증([소유자원-접근제한])과 `정원>0`·`시작일<종료일` 유효성만 서버에서 강제, API 계약은 Sprint 2 내 후속 설계 | 세션 등록에도 전체관리자 승인 단계 추가 — 이미 승인된 컨퍼런스 안에서 정원 조정마다 관리자 병목이 생겨 기각 | Sprint 2 |

미래 기술을 완성된 구조에 그리지 않습니다.