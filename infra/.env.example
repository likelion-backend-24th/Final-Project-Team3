MYSQL_ROOT_PASSWORD=changeme
MEMBER_DB_USER=member_svc
MEMBER_DB_PASSWORD=changeme
CONFERENCE_DB_USER=conference_svc
CONFERENCE_DB_PASSWORD=changeme
RESERVATION_DB_USER=reservation_svc
RESERVATION_DB_PASSWORD=changeme
JWT_SECRET=changeme
PG_CREDENTIAL_ENCRYPTION_KEY=changeme
PORTONE_STORE_ID=                             # 선택: 넣으면 기동 시 DB에 자동 등록(어드민 화면 입력값을 덮어씀)
PORTONE_CHANNEL_KEY=
PORTONE_API_SECRET=
PORTONE_WEBHOOK_SECRET=                       # 웹훅 URL 등록 후 발급, 비워둬도 됨
MAIL_USERNAME=
MAIL_PASSWORD=                                # Gmail 앱 비밀번호, 공백 없이 16자리로 입력 (구글 계정 2단계 인증 켠 뒤 발급)
GEMINI_API_KEY=changeme
NTS_SERVICE_KEY=
SOCIAL_MODE=mock                              # real: 실제 Google/Kakao 서버 검증 / mock: 검증 스킵(키 없이 기동할 때)
GOOGLE_OAUTH_CLIENT_ID=                       # 백엔드(member-service)용. 프론트와 동일한 값
VITE_GOOGLE_CLIENT_ID=                        # 프론트 빌드용. GOOGLE_OAUTH_CLIENT_ID와 동일한 값을 넣는다
VITE_KAKAO_JS_KEY=                            # Kakao Developers 앱의 JavaScript 키