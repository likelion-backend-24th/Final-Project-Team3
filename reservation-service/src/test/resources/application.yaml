spring:
  application:
    name: reservation-service-test
  datasource:
    url: jdbc:mysql://localhost:3306/reservation_service
    username: reservation_svc
    password: reservation1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true